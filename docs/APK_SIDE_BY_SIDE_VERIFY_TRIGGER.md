# APK Side-by-Side Verification Trigger

Build the exact frozen V3.6.0 baseline commit `b907d7aeced266e914c360c2f70b475dd9e42b62` as a standalone Release APK.

The GitHub runner temporarily changes only the Android applicationId and app display name so the APK can be installed next to the existing production app without touching its data.

Trigger revision: 1
